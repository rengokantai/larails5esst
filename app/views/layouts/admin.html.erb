<!DOCTYPE html>
<html>
  <head>
    <title>larails5esst <%= @page_title || "Admin" %></title>
  </head>
  <body>

  <% if !flash[:notice].blank? %>
    <div class="notice">
      <%= flash[:notice] %>
    </div>
  <% end %>

  <!-- before yield -->
  <%= yield %>
  <!-- after yield -->

  </body>
</html>
